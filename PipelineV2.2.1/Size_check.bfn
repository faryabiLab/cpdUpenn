#!/bin/bash
Size_check ()
{
in_file=$1;
in_file_size=`stat --printf='%s' $in_file`;
if [ $in_file_size > 1024 ];
then
	echo 'The size of '$in_file' is OK..continuing with PipeScript'; else echo 'The size of '$in_file' is not OK...halting the PipeScript';
	exit 66;
fi
}
